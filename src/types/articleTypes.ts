export type TGetArticle = {
  createdAt: string;
  region: string;
  temp_now: number;
  temp_feels: number;
  temp_min: number;
  temp_max: number;
  description: string;
  image: string;
  id: string;
  userId: string;
};
